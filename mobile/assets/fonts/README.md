# Fonts

The project includes Inter Regular, Medium and Semibold through `@expo-google-fonts/inter`.

Headings use Inter Semibold directly. Font assets are registered in `src/design-system/fonts.ts` and bundled locally for native and web builds.
